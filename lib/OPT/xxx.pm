package OPT::xxx;
use strict;
use warnings;
use Exporter qw(import);
our @EXPORT_OK = qw( launchChainSputnik );

sub launchChainSputnik(\[$@%]);

sub launchChainSputnik {

	my ($symbol, $table, $simulation) = @_;
	print "i work fool";
}
1;
